version: '3.8'

services:
  maptoposter:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Persist generated posters
      - ./posters:/app/posters
      # Persist map data cache
      - ./cache:/app/cache
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
